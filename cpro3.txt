// SPDX-Licence-Indentifir:MIT
// SPDX-License-Identifier: MIT

pragma solidity ^0.8.0;

contract basicSolididty{
    //uint256 public myuint;
   // int public myint;
   // bool public mybool;
    //string public mystring;
    //address public myaddress;

    uint256 fav;
    struct People{
        uint256  favnumber;
        string name;
    }
    People[] public people;


    mapping(string => uint256) public nametofavnumber;
    function store(uint256 number) public {
        fav=number;
    }
    function retrive() public view returns (uint256){
        return fav;
    }

    function adddData(string memory name,uint256 nav) public
    {
        people.push(People(nav,name));
        nametofavnumber[name]=nav;
    }
}